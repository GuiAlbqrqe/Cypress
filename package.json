{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:open:chrome": "cypress open --browser electron",
    "cy:run:chrome": "cypress run --browser electron",
    "cypress:open:dev": "cypress open --config-file config-dev.js --browser electron",
    "cypress:open:qa": "cypress open --config-file config-qa.js --browser electron"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "cypress": "^15.10.0",
    "cypress-mochawesome-reporter": "^4.0.2",
    "cypress-multi-reporters": "^2.0.5",
    "dotenv": "^17.3.1",
    "mocha-junit-reporter": "^2.2.1"
  }
}
